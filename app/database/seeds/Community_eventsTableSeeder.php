<?php

class Community_eventsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('community_events')->truncate();

		$community_events = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('community_events')->insert($community_events);
	}

}
